# LifeOS — STATE.md

> One screen. Generated by `hermes-state` from git and the filesystem —
> do not hand-edit, it is overwritten. Every line below is derived, not authored.

_Generated 2026-08-01 12:46 CEST · HEAD `36c9f0d` on `main`_

## Phase
**Idle** — no approved work queued, nothing unmerged.

## Working tree
Clean.

## Merge queue
`hermes` exists and is level with `main` — nothing to merge.

## Brief queue
**Completed** (`docs/done/`): 2 — food-intake-24h-graph.md, intake-food-last-24h.md

## Gates
Run them with `hermes-gates <path>`; they are executed, not advisory.
```
  build  find js scripts tests \( -name '*.js' -o -name '*.mjs' \) -print0 | xargs -0 -n1 node --
  test   TZ=UTC node --test tests/*.test.mjs && TZ=America/Los_Angeles node --test tests/*.test.m
  lint   node scripts/check-tz.mjs
  smoke  node scripts/smoke.mjs
  graph  python3 .understand-anything/gen_graph.py --check
```

## Knowledge graph
66 nodes / 163 edges, generated at commit `5752a2ed`. Freshness is enforced by the `graph` gate.

## Open decisions
_No ADRs recorded._

## Run records
1 in `docs/runs/`, most recent first:
- `2026-08-01-workflow-hardening.md`

## Recent commits
```
  36c9f0d chore: regenerate STATE.md from real repository state
  c7e796a fix: Budapest timezone correctness + real gates
  5752a2e fix: meal log + intake list use rolling 24h window (not calendar day)
  818c83b feat: add 24h hourly graphs to food & intake cards
  b6164d4 docs: add hermes-init AGENTS contract proposal
  ee6c98f docs: add AGENTS.md and CLAUDE.md (identical project docs)
  b944855 chore(graph): refresh knowledge graph for LifeOS
  70b9b0a chore: align Hermes command refs to hyphen form (hermes-init/plan/understand); tighten test gate
```
