        # AGENTS.md / CLAUDE.md — PROPOSED for LifeOS

        > ⚠️ **PROPOSAL ONLY.** This file was generated by `/hermes-init`.
        > It is NOT written to AGENTS.md or CLAUDE.md. Those files are
        > proposal-only per the Hermes Protocol. You must merge this content
        > yourself if you want it active.

        ---

        ## Hermes Contract

        ### Read on every execution run, in this order
        1. AGENTS.md, CLAUDE.md              (constitution — read-only)
        2. docs/GRAPH-INDEX.md               (project map index; full graph at .understand-anything/)
        3. docs/STATE.md                     (current truth)
        4. docs/ROADMAP.md                   (active phase, known-broken, owner decisions)
        5. docs/decisions/ — only ADRs referenced by the task
        6. docs/runs/ — 3 most recent only
        7. docs/active/<task>.md             (the task at hand)

        Never read: docs/done/**, docs/runs/archive/**, docs/briefs/**,
        .understand-anything/knowledge-graph.json (query via `hermes graph` instead).

        ### Write targets
        Allowed:      docs/active/*, docs/runs/*, docs/STATE.md, source within declared scope
        Phase A only: docs/decisions/*, docs/briefs/*
        Proposal only (PR, never direct commit): AGENTS.md, CLAUDE.md, docs/GRAPH-INDEX.md
        Forbidden:    .env*, secrets/**, **/migrations/**, CI config, main/master, .hermes/config.yml

        ### Git rules
        - Work happens on the `hermes` branch. Never write to `main`.
        - Commit on green gates with the trailer block. Push. No merge unless human-commanded.
        - Merge is human command only: "merge #<n>" or "merge everything through <SHA>".
        - Never resolve a merge conflict. Abort and escalate with paths.

        ### Verification gates
        - build:  node --check js/*.js
        - test:   echo 'FATAL: no test suite configured — update gates in .hermes/config.yml' && false
        - lint:   node --check js/*.js
        - smoke:  python3 -c "
import re
with open('index.html') as f: h = f.read()
assert len(re.findall(r'<section\b', h)) == len(re.findall(r'</section>', h)) == 6, 'section mismatch'
for fn in ['js/app.js','js/auth.js','js/nav.js','js/dashboard.js','js/quicklog.js','js/charts.js','js/history.js','js/cards.js','js/utils.js','js/config.js']:
    with open(fn) as f: pass
print('OK')
"

        ### Do-not-touch
        - AGENTS.md, CLAUDE.md, .hermes/config.yml
        - .env, .env.*, secrets/**, **/migrations/**, .github/workflows/**

        ### One-way doors — never auto-approvable
        - Schema changes (tables, columns, migrations)
        - Public API changes
        - Breaking dependency upgrades

        ### Anti-zombie-code rule
        Before creating any file or symbol, run `hermes graph find <concept>`.
        If no graph exists, fall back to `grep -r <concept> <project_root>`.
        Report what came back. Create only if empty.

        ### Commands
        ```
        /hermes-plan [project]        Phase A
        /hermes-execute [project]     Phase B — one run
        /hermes-merge <ref>           human-only merge, commit order
        /hermes-status [project]      project status
        /hermes-halt [project]        write .hermes/HALT
        /hermes-understand [project]  graph refresh + GRAPH-INDEX regenerate
        /hermes-init [project]        scaffold protocol scaffolding
        ```

        Stack detected: **vanilla-js-spa**
